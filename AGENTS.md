# AI Infra 学习项目协作约定

本仓库既保存学习产物，也保存跨 Codex 会话的项目记忆。任何新会话都不要依赖聊天历史；以仓库中的文档为准。

## 仓库内容边界

仓库保留以下内容：

1. 用户本地收藏的 `.resources/` 学习资料；该目录被 Git 忽略。
2. 项目管理系统，包括 `AGENTS.md`、`README.md`、`docs/`、`.gitignore` 和 Git 元数据。
3. `notes/` 中按日和按主题组织的学习笔记。
4. `experiments/` 中围绕单一问题设计的小型验证实验。
5. `projects/` 中持续演进并面向最终展示的贯穿项目。
6. 本地临时目录 `.tmp/`；该目录被 Git 忽略，不承载需要复现或跨会话保存的内容。
7. 应用自动维护的空 `.agents/`、`.codex/` 目录。

不新增无关顶层目录、编辑器配置或构建产物。项目管理、路线和跨会话记忆写入 `docs/`；学习过程和知识沉淀写入 `notes/`；一次性验证写入 `experiments/`；稳定实现写入 `projects/`。构建产物、缓存和大型 Profiler 原始文件不得提交。

## 会话启动协议

开始实质工作前，按顺序完成以下步骤：

1. 运行 `git status --short --branch`，确认分支和用户已有的未提交修改。
2. 阅读 `docs/PROJECT.md`，理解长期目标、范围和工作原则。
3. 阅读 `docs/CURRENT.md`，了解当前进度、下一步和阻塞项。
4. 阅读 `docs/ROADMAP.md`，确认本次任务处于哪个阶段。
5. 浏览 `docs/DECISIONS.md` 中与任务相关的决策；需要背景时再查 `docs/SESSIONS.md`、`docs/README.md` 和相关学习文档。
6. 用一两句话向用户复述当前状态和本次准备推进的事项；若用户的新指令与文档冲突，以用户的新指令为准，并在完成后同步文档。

## 文档职责

- `README.md`：项目入口，面向人类读者。
- `docs/PROJECT.md`：稳定的项目目标、边界和原则。仅在方向变化时修改。
- `docs/CURRENT.md`：唯一的短期状态快照。每次实质工作后检查是否需要更新。
- `docs/ROADMAP.md`：阶段、里程碑与待办，不记录流水账。
- `docs/DECISIONS.md`：影响后续工作的关键决策，按时间追加，保留被替代决策的历史。
- `docs/SESSIONS.md`：重要会话的精简交接记录，按时间倒序追加。
- `docs/README.md`：文档索引和可复用学习文档的写作规范。
- `docs/RESOURCES.md`：本地收藏资料的分工、取用方法和使用边界。
- `notes/`：按日记录学习过程，并按主题沉淀知识、重难点和踩坑。
- `experiments/`：验证单个知识点或性能假设的小型实验；不得成为贯穿项目的运行时依赖。
- `projects/`：稳定实现、测试、Benchmark、结果报告和最终展示材料。
- `.tmp/`：本地杂项和短期中间文件，整体忽略；任何结论、代码或结果一旦需要复现，必须迁移到正式目录。

## 会话收尾协议

完成实质工作后：

1. 先验证产物，再更新记忆文档；不要把计划中的结果写成已经完成。
2. 若本次包含实质学习，Codex 无需等待用户提醒，自动更新当日 `notes/daily/YYYY-MM-DD.md`；知识形成稳定理解时再更新 `notes/topics/`。只记录有对话、代码或实验支持的学习表现，并区分独立完成、提示后完成和待验证。
3. 更新 `docs/CURRENT.md` 的“最后更新”“已完成”“正在进行”“下一步”和“阻塞项”。保持它短小，清理失效信息。
4. 若里程碑状态发生变化，更新 `docs/ROADMAP.md`。
5. 若做出了会约束未来工作的选择，按模板在 `docs/DECISIONS.md` 追加一条记录；不要静默改写旧决策。
6. 若本次工作产生了值得下一会话知道的上下文，在 `docs/SESSIONS.md` 顶部追加交接记录，并链接具体文件。
7. 在最终回复中说明完成内容、验证结果和明确的下一步；引用仓库文件时使用可点击的绝对路径。

纯问答、只读检查或没有改变项目状态的操作，无需为了留痕而机械更新文档。

## 记录规范

- 区分事实、决定、假设和待验证问题；不确定内容必须显式标注。
- 使用 `YYYY-MM-DD` 日期，时间语义以 `Asia/Shanghai` 为准。
- 文档保持摘要化，通过链接指向代码、实验或学习文档，避免复制大段内容。
- 先更新现有条目，只有在存在独立主题时才新建文件。
- 不记录密钥、令牌、个人隐私或仅存在于本机的敏感配置。
- 尊重用户的未提交修改，不把无关改动混入当前任务。
