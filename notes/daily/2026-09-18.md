# 2026-09-18

## 本次目标

继续 L0 / M0，从 Host→Device 输入拷贝推进至 Kernel 启动与结果回传。

## 动手与证据

- 用户按接口提示自行写出 h_A→d_A、h_B→d_B 两次 cudaMemcpy，目的/源地址、bytes 和 cudaMemcpyHostToDevice 均正确。
- Codex 按既有授权为两次调用接入状态检查和失败清理，保留用户的拷贝参数；不再要求用户反复编写辅助判断。
- nvcc 编译链接成功，仅 h_C 未使用警告；沙箱外运行无输出、退出码 0，三次分配、两次输入拷贝和释放均报告成功。
- 证据：[源码](../../experiments/l0-vector-add-cuda/main.cu)、[实验记录](../../experiments/l0-vector-add-cuda/README.md)。此为输入拷贝阶段的检查；后续完整运行结果见下。

## 理解与待验证

- 用户已在提示后正确实现 Host→Device 输入拷贝；独立解释传输参数与方向的能力尚未额外检查。
- 已按提示使用 Kernel 启动语法：函数名<<<块数, 每块线程数>>>(参数)，本例 2 块×4 线程处理 6 个元素。

## 首次 GPU 数组相加运行成功

- 用户按提示自行补 vector_add<<<2,4>>>(d_A,d_B,d_C,N)、结果 Device→Host 回传及打印；参数、方向和位置正确。
- Codex 按授权补 cudaGetLastError、cudaDeviceSynchronize 和回传状态检查及失败清理。
- 本机查询计算能力为 12.0，显式 nvcc -arch=sm_120 编译链接无警告。沙箱外运行退出码 0，六个输出 11、22、33、44、55、66 全部符合预期；全部检查调用成功。
- 本例输入→GPU→输出流程已跑通；仅固定用例经输出比较，未实现自动正确性测试或性能测量，未故障注入。辅助代码由 Codex 编写，不能将同步机制和错误处理记为用户已掌握。

## 当日收尾

- 用户决定今天结束学习；最小 CUDA 数组相加实验正式完成，不因后续计时任务继续延长本实验。
- 验证结果：本轮最终代码编译无警告，GPU 运行六个结果正确、退出码 0，复现方法已保存。
- 已表现：按提示自行完成 Kernel、输入拷贝、启动、回传与打印。辅助错误处理由 Codex 完成；异步/同步和计时理解仍待验证。
- 最后提出的“CPU 只包住启动语句计时是否等于 Kernel 执行时间”尚未回答，不记录为掌握。

## 下次起点

从异步启动、同步与计时范围开始新的学习内容，不重做已完成实验，不再反复布置错误判断。更大的 L0 / M0 仍需自动正确性检查、基础计时和贯穿项目整理；这些不影响当前实验已完成的状态。

