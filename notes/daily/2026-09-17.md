# 2026-09-17

## 本次目标

启动 L0 / M0，初步盘点环境，建立 CPU（Host）、GPU（Device）、编译工具与驱动的区别。

## 动手与证据

以下检查由 Codex 执行，不代表用户已独立完成：

- `uname -srmo`、`/etc/os-release`：Ubuntu 26.04.1 LTS，Linux 7.0.0-31-generic，x86_64。
- `lspci -nnk -d 10de:`：RTX 5060 Max-Q / Mobile，使用 nvidia 内核驱动。
- `nvcc --version`：13.4.59；`g++ --version`：15.2.0。后续 Kernel 目标文件编译已通过，链接与 GPU 运行尚未验证。
- 沙箱内 `nvidia-smi` 无法连接驱动，且看不到 `/dev/nvidia*`；经批准在沙箱外执行成功，驱动 595.91.07，显示 CUDA Version 13.2，总显存 8151 MiB。当时使用 41 MiB，为瞬时观测。
- 当前 PATH 未找到 cmake；系统 Python 的 `python3 -m pip show torch` 因缺少 pip 失败，不能据此判断其他环境没有 PyTorch。

## 踩坑与修正

- 沙箱内 GPU 查询失败不能直接推断本机驱动损坏；沙箱外查询成功，访问限制是本次现象的重要条件。
- Toolkit 与驱动报告的 CUDA 版本不同；尚未验证当前组合能否完成目标程序的编译与运行，不据此直接升级或重装。

## 学习状态

- 已用数组逐元素相加说明 Host/Device 分工及显式数据拷贝流程。
- 在前述讲解后，用户回答“GPU 算完之后还要将计算结果搬到内存中”，正确指出 CPU 使用结果前的数据回传步骤；导师补充准确表述为“从 GPU 显存复制到主机内存”。
- 迁移检查：针对 GPU 连续计算 C=A+B、D=C×2，用户独立解释 C 可留在显存供后一步使用，无须往返复制；已能在这一场景下判断中间结果的数据流。导师补充表述：是 CPU 不需要读取 C，GPU 仍需要 C。
- 输出缓冲区检查：用户回答“不需要，直接用 GPU 初始化显存的对应区域即可”，正确否定主机清零后复制的必要性，但“初始化”是否指额外清零尚不明确。导师澄清：若每个 C[i] 都由赋值覆盖，分配后可直接计算写入，不需要额外清零；随后已进行读取旧值的 += 对比。
- 对比检查：用户独立指出 += 依赖 C 的初始值，因此不能忽略初值。导师进一步澄清要求是“初值有效且符合计算意图”，不是始终保持原值；从零累加时应先设零。
- 用户独立完成 [CPU 赋值/累加实验](../../experiments/l0-assignment-vs-accumulation/main.cpp)，初始化、索引范围与输出均正确；首次检查时未记录具体数值预测；后续用户已补充正确输出注释，不将补记当作事前预测。Codex 使用 G++ 15.2.0、C++17 和警告选项重新编译，无警告，运行输出为 (11,111)、(22,122)、(33,133)、(44,144)，与赋值/累加语义一致；[复现与结果](../../experiments/l0-assignment-vs-accumulation/README.md)。
- 源目录已有可执行文件 main，保留用户文件并添加精确 Git 忽略规则；Codex 构建产物放入 .tmp。此时尚未进行 CUDA 实验，后续进展见下。
- 本地概念材料：`.resources/repos/AIInfraGuide/docs/guides/模块二-CUDA编程与算子优化/1.2-CUDA编程模型.md`，先取用异构计算部分。
- 官方编译说明：https://docs.nvidia.com/cuda/cuda-compiler-driver-nvcc/

## 线程映射入门（实现已检查，GPU 运行待验证）

- 为编写最小 Kernel 按需引入 L1 的一维线程编号：Thread、Block、Grid，以及 i = blockIdx.x × blockDim.x + threadIdx.x。当前仍处于 L0 / M0，未完成 L1。
- 教学例子为 6 个元素、2 个 Block、每个 Block 4 个线程；通过多出的线程引出边界检查。用户回答“4 和 5 执行加法，6 和 7 跳过；i < N”，正确给出了全局下标的有效范围与边界条件，但问题要求的是块内 threadIdx.x。导师澄清块 1 中局部编号 0、1 执行，2、3 跳过；后续用户在 Kernel 中正确使用三个内置量计算全局下标。
- 已核对 NVIDIA 官方说明：https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/intro-to-cuda-cpp.html

- 用户询问 const int* 与 const int& 的区别；导师说明数组访问入口与单个整数引用的区别，尚无独立复述证据。
- 用户按接口和步骤提示自行编写 [vector_add Kernel](../../experiments/l0-vector-add-cuda/main.cu)，参数、一维索引与边界均正确。Codex 用 nvcc -c 编译成功，无诊断输出；没有修改用户源码。仅验证编译，未验证 GPU 运行，见[记录](../../experiments/l0-vector-add-cuda/README.md)。

- 用户自行补齐 Host main：N=6、两个输入数组、输出数组与 bytes=N*sizeof(int) 均正确；h_C[6] 可改为 h_C[N] 减少重复常量。Codex 未改源码。
- Codex 编译并链接成功；四条未使用变量警告与当前阶段一致。未执行程序，未验证 GPU 运行。下一步先用 d_A 完成分配—检查—释放，再扩展。

- 用户追问 &d_A 与返回状态：导师解释指针变量地址、cudaError_t 和 cudaSuccess；用户随后自行用状态变量接收 cudaMalloc 返回值，并实现成功/失败分支。
- 当前代码已调用 cudaFree，但丢弃其返回值，成功分支返回的仍是 cudaMalloc 状态；释放错误处理未完成。缺少显式 cuda_runtime.h，nvcc 当前仍能编译。
- Codex 编译链接成功，仅三个主机数组未使用警告；沙箱外运行无输出、退出码 0，可确认分配成功与释放调用已执行，不能确认释放返回成功。尚未启动 Kernel；源码未修改。

- 修正复测：用户按反馈自行分别保存并检查 status_malloc/status_free，报错信息带操作名。Codex 重新编译链接成功，沙箱外运行无输出、退出码 0；本次分配和释放均成功。错误路径仅静态审查，未触发。仍未显式包含 cuda_runtime.h，后续补充。

- 用户表示三层嵌套难以编写；导师提供“失败提前返回”的流程提示并缩小为两块任务。用户随后自行写出完整三块版本，分配检查及失败路径清理范围正确，显式头文件已补充。这是提示后完成，不记录为无提示独立设计。
- Codex 编译链接成功；沙箱外运行无输出、退出码 0，三次分配均成功。扩展版本遗漏全部 cudaFree 状态检查，尚不能确认释放结果；需要累计失败标志并在结尾统一返回，避免释放失败后跳过其余清理。

- 累计标志实现尚未通过：用户将每次 cudaFree 返回值直接赋给 bool is_free_success，混淆单次状态与累计失败标志，且后续成功会覆盖先前失败；分配失败分支仍未报告释放错误。导师用“释放 A 失败、B/C 成功”作静态反例，不作为真实故障记录。
- 本轮编译链接通过（仍有三个数组未使用警告），未运行 GPU；先修正状态覆盖逻辑。用户尚未表现出对累计标志的正确实现，不记录为掌握。

- 用户明确要求由 Codex 补齐错误判断并继续主线；Codex 增加 check_cuda/release_buffers，完成全部清理检查并保留分配失败结果。此部分为 Codex 实现，不记为用户掌握。
- 编译链接成功，沙箱外运行无输出、退出码 0，正常分配/释放均成功；故障路径仅代码审查。已记录 D-011，避免后续再次用错误处理反复阻塞主线。

## 下一步

用户学习并补两次 Host→Device 输入拷贝；Codex 负责接入重复错误处理，再推进 Kernel 启动。M0 尚未完成。
